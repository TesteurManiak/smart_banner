import 'package:flutter/material.dart';

class BannerColorPalette {
  const BannerColorPalette._();

  static const backgroundIOS = Color(0xFFf2f2f2);
  static const backgroundAndroid = Color(0xFF3d3d3d);
  static const shadowIOS = Color.fromRGBO(0, 0, 0, 0.2);
  static const buttonIOS = Color(0xFF0c71fd);
  static const titleIOS = Color(0xFF4d4d4d);
  static const titleAndroid = Colors.white;
  static const descriptionIOS = Color(0xFF6a6a6a);
  static const descriptionAndroid = Color(0xFFcccccc);
}
